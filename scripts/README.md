# Scripts

Place project utilities here:

- PDF/drawing ingestion
- OCR and metadata extraction
- Knowledge-graph generation
- Validation and quality checks
- Visual comparison and diff generation
- Build and migration helpers

Legacy root-level scripts are retained temporarily. New scripts should use clear `snake_case` names and document their CLI usage, inputs, outputs and dependencies.
